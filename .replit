language = "python3"
run = "echo $(date +'%Y/%d/%m_%H:%M:%S START') > restart.log; npm install nodemon; npx nodemon -L -x './venv/bin/python app.pyc || { rm -f .git/index.lock; git pull origin main --force; git reset --hard origin/main; ./venv/bin/pip install -r requirements.txt; echo $(date +'%Y/%d/%m_%H:%M:%S CRASH') >> restart.log; };' --watch restart.log;"

[env]
VIRTUAL_ENV = "/home/runner/${REPL_SLUG}/venv"
PATH = "${VIRTUAL_ENV}/bin"
PYTHONPATH="${VIRTUAL_ENV}/lib/python3.8/site-packages"
REPLIT_POETRY_PYPI_REPOSITORY="https://package-proxy.replit.com/pypi/"
MPLBACKEND="TkAgg"